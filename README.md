# poneill.github.io

Build output only. This repository contains the rendered static site served at
<https://poneill.github.io/> — it is generated, not edited by hand.

The Quarto source lives in a separate private repository. Changes made here will
be overwritten by the next deploy.

The previous Jekyll site that lived here through 2023-07-30 is archived in full
at `poneill/old-website`.
